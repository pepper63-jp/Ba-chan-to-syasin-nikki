# 写真日記 PWA：AQUOS でインストール・起動しない場合の対処

## 起きていた症状

- インストール中「写真日記をインストールしています」のあと、ホームに追加されない／反応がない
- ホームに追加できても「AQUOS ホームが繰り返し停止しています」と出る
- アイコンをタップしても音だけしてアプリが開かない
- アイコンを長押しして移動しようとするとまた AQUOS ホームが落ちる

## 主な原因（今回の修正で対応した内容）

1. **アイコン形式**  
   マニフェストで **favicon.ico** を 192px / 512px の PWA アイコンとして指定していました。  
   - .ico は PWA の公式推奨ではなく、**Android の一部ランチャー（AQUOS ホームなど）が正しく扱えず、クラッシュや無反応の原因**になります。  
   - そのため **PNG（icon-192.png / icon-512.png）に変更**しました。

2. **start_url**  
   `"./"` の相対パスだと、ホーム画面から起動したときに「どこを開くか」が端末によって曖昧になり、**白画面・無反応**になりやすいです。  
   **`"/"`（絶対パス）に変更**しました。

3. **scope・id の不足**  
   PWA の範囲とアプリの一意性がはっきりしておらず、インストールや起動が不安定になることがあります。  
   **`scope: "/"` と `id: "/"` を追加**しました。

## 今回の修正内容（開発側）

- `manifest.webmanifest` の修正  
  - `start_url`: `"./"` → `"/"`  
  - `scope`: `"/"` を追加  
  - `id`: `"/"` を追加  
  - アイコンを **PNG のみ**（`/icon-192.png`, `/icon-512.png`）に変更  
  - `orientation`: `"portrait-primary"`（端末の「自動回転オフ」と縦持ち利用に合わせる。以前の `"any"` は PWA が勝手に回転することがあった）
- `public` フォルダに **icon-192.png** と **icon-512.png** を配置

修正を反映したうえで、**Firebase などに再デプロイ**してください。

## ユーザー（利用者）がやること

1. **一度 PWA を削除する**  
   - ホーム画面の写真日記アイコンを長押し → 「削除」や「アンインストール」で削除。  
   - または 設定 → アプリ → 写真日記（またはブラウザの「ホームに追加」で作ったショートカット）→ アンインストール。

2. **ブラウザのキャッシュを消す（推奨）**  
   - Chrome：設定 → プライバシーとセキュリティ → 閲覧履歴データの削除  
   - 「キャッシュされた画像とファイル」にチェック → 削除。  
   古いマニフェストやアイコンが残っていると、同じ不具合が出ることがあります。

3. **サイトを再度開いて「ホームに追加」**  
   - 写真日記のサイトを Chrome で開く → メニュー（⋮）→ 「ホーム画面に追加」または「アプリをインストール」。  
   - 新しいマニフェストと PNG アイコンが使われ、AQUOS ホームが落ちにくくなります。

4. **それでも AQUOS ホームが落ちる場合**  
   - **ランチャーを変える**：  
     設定 → アプリ → デフォルトアプリ → ホームアプリで、  
     「AQUOS ホーム」以外（例：Google の「Pixel Launcher」や「Microsoft Launcher」など）を選ぶと、PWA が安定することがあります。  
   - または、**ブラウザの「ホームに追加」ではなく、Chrome の「アプリ」一覧から起動**する方法もあります（メニュー → 「アプリ」→ 写真日記）。

## まとめ

- **原因**：PWA のアイコンに .ico を使っていたことと、`start_url` が相対パスだったことなどが、AQUOS ホームでの不具合や無反応につながっていました。  
- **対処**：マニフェストを修正し、アイコンを PNG にし、再デプロイしたうえで、利用者に一度アンインストール→キャッシュ削除→再インストールしてもらってください。  
- それでも問題が出る場合は、別のホームアプリ（ランチャー）を使うと改善することがあります。
